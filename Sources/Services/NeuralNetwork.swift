class NeuralNetwork {
    let inputSize, layerSize, numberOfLayers: Int
    
    init(inputSize: Int, layerSize: Int, numberOfLayers: Int) {
        self.inputSize = inputSize
        self.layerSize = layerSize
        self.numberOfLayers = numberOfLayers
    }
    
    
}
